//! CLI stub — WASM-only project.  Build with `trunk serve` or `wasm-pack`.
//! The real entry point is in `lib.rs` with `#[wasm_bindgen(start)]`.

fn main() {
    println!("b00t-ui-wasm: use `trunk serve` for WASM, or `cargo test` for tests");
}
