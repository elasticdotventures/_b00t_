[package]
name = "ufo-types"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "UFO (Unified Foundational Ontology) stereotypes, Satisfies<T> constraint trait, and ISO standard type wrappers for the Tax-Lawyer platform"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
