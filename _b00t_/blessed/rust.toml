# Blessed Rust crates — sourced from blessed.rs (nicoburns/blessed-rs)
# Auto-generated. Regenerate: just blessed-sync

[[crate]]
category = "Common/General"
use_case = "Random numbers"
recommended = ["rand", "fastrand"]
notes = {"rand": "De facto standard random number generation library split out from the standard library", "fastrand": "A simple and fast random number generator (but not cryptographically secure)."}

[[crate]]
category = "Common/General"
use_case = "Time & Date"
recommended = ["jiff", "time", "chrono"]
notes = {"jiff": "A datetime library with an API based on JavaScript's Temporal, automatic tzdb integration and convenient daylight saving time calendar arithmetic, but that currently lacks some of the ecosystem integration that time and chrono have.", "time": "A widely used datetime library with a focused API, a flexible parsing/formatting DSL and compile time datetime literals.", "chrono": "A widely used datetime library with built-in time zone support, a traditional parsing/formatting DSL and some support for calendar arithmetic. It also has a conservative a MSRV policy. However, it is <a href=\"https://github.com/chronotope/chrono/issues/1768\">soft-deprecated</a>."}

[[crate]]
category = "Common/General"
use_case = "Regular Expressions"
recommended = ["regex", "fancy-regex"]
notes = {"regex": "De facto standard regex library. Very fast, but does not support fancier features such as backtracking.", "fancy-regex": "Use if need features such as backtracking which regex doesn't support"}

[[crate]]
category = "Common/General"
use_case = "UUIDs"
recommended = ["uuid"]
notes = {"uuid": "Implements generating and parsing UUIDs and a number of utility functions"}

[[crate]]
category = "Common/General"
use_case = "Temporary files"
recommended = ["tempfile"]
notes = {"tempfile": "Supports both temporary files and temporary directories"}

[[crate]]
category = "Common/General"
use_case = "Gzip (de)compression"
recommended = ["flate2"]
notes = {"flate2": "Uses a pure-Rust implementation by default. Use feature flags to opt in to system zlib."}

[[crate]]
category = "Common/General"
use_case = "Insertion-ordered map"
recommended = ["indexmap"]
notes = {"indexmap": "A HashMap that separately keeps track of insertion order and allows you to efficiently iterate over its elements in that order"}

[[crate]]
category = "Common/General"
use_case = "Stack-allocated arrays"
recommended = ["arrayvec", "smallvec", "tinyvec"]
notes = {"arrayvec": "Arrays that are ONLY stack-allocated with fixed capacity", "smallvec": "Arrays that are stack-allocated with fallback to the heap if the fixed stack capacity is exceeded", "tinyvec": "Stack allocated arrays in 100% safe Rust code but requires items to implement the Default trait."}

[[crate]]
category = "Common/General"
use_case = "HTTP Requests"
recommended = ["reqwest", "ureq"]
notes = {"reqwest": "Full-fat HTTP client. Can be used in both synchronous and asynchronous code. Requires tokio runtime.", "ureq": "Minimal synchronous HTTP client focused on simplicity and minimising dependencies."}

[[crate]]
category = "Common/Error Handling"
use_case = "For applications"
recommended = ["anyhow", "color-eyre"]
notes = {"anyhow": "Provides a boxed error type that can hold any error, and helpers for generating an application-level stack trace.", "color-eyre": "A fork of anyhow that gives you more control over the format of the generated error messages. Recommended if you intend to present error messages to end users. Otherwise anyhow is simpler."}

[[crate]]
category = "Common/Error Handling"
use_case = "For libraries"
recommended = ["thiserror"]
notes = {"thiserror": "Helps with generating boilerplate for enum-style error types."}

[[crate]]
category = "Common/Logging"
use_case = "Text-based logging"
recommended = ["tracing", "log"]
notes = {"tracing": "Tracing is now the go-to crate for logging.", "log": "An older and simpler crate if your needs are simple and you are not using any async code."}

[[crate]]
category = "Common/Logging"
use_case = "Structured logging"
recommended = ["tracing", "slog"]
notes = {"tracing": "Tracing is now the go-to crate for logging.", "slog": "Structured logging"}

[[crate]]
category = "Common/Language Extensions"
use_case = "Iterator helpers"
recommended = ["itertools"]
notes = {"itertools": "A bunch of useful methods on iterators that aren't in the stdlib"}

[[crate]]
category = "Common/Language Extensions"
use_case = "Macro helpers"
recommended = ["syn", "quote", "pastey", "darling"]
notes = {"syn": "Parse rust source code", "quote": "Quasi quoting rust (useful for interpolating generated code with literal code)", "pastey": "Concatenating and manipulating identifiers", "darling": "Derive macro to easily parse derive macro inputs"}

[[crate]]
category = "Common/Language Extensions"
use_case = "Safe type casts"
recommended = ["bytemuck", "zerocopy"]
notes = {"bytemuck": "", "zerocopy": ""}

[[crate]]
category = "Common/Language Extensions"
use_case = "Bitflags"
recommended = ["bitflags"]
notes = {"bitflags": "Strongly typed bitflag types"}

[[crate]]
category = "Common/Serialization"
use_case = "General-purpose / format agnostic"
recommended = ["serde"]
notes = {"serde": "De facto standard serialization library. Use in conjunction with sub-crates like `serde_json` for the specific format that you are using (JSON, YAML, etc). Supports a variety of both text and binary protocols, including self-describing ones."}

[[crate]]
category = "Common/Serialization"
use_case = "Non-self-describing, external schema file"
recommended = ["serde_json", "toml", "prost", "capnp", "flatbuffers"]
notes = {"serde_json": "A fast and heavily optimized JSON encoder and decoder.", "toml": "Serde compatible encoder for Tom's obvious, minimal language. Very commonly used for human-friendly configuration data.", "prost": "Protocol buffer library with strong ergonomics and wide industry adoption. Commonly used with gRPC / `tonic`. Use in conjunction with `protox` to avoid dependency on `protoc`.", "capnp": "Cap'n Proto library. Offers zero-copy usage mode that is optimal for large messages, along with other improvements over Protocol Buffers. Tradeoffs are less wide usage and less developer-friendly.", "flatbuffers": "Flatbuffers library. Also offers zero-copy usage and is more widely used than cap'n proto, with further cost to ergonomics."}

[[crate]]
category = "Common/Serialization"
use_case = "Non-self-describing, no external schema file"
recommended = ["postcard", "rkyv"]
notes = {"postcard": "<code>no_std</code> focused Serde serializer/deserializer, aimed at constrained environments.", "rkyv": "Fast zero-copy deserialization framework that allows arbitrary field types and safe zero-copy mutation."}

[[crate]]
category = "Common/System"
use_case = "Memory mapping files"
recommended = ["memmap2"]
notes = {"memmap2": "The older memmap crate is unmaintained."}

[[crate]]
category = "Common/System"
use_case = "Libc"
recommended = ["libc"]
notes = {"libc": "Bindings for directly calling libc functions."}

[[crate]]
category = "Common/System"
use_case = "Windows (OS)"
recommended = ["windows"]
notes = {"windows": "The official Microsoft-provided crate for interacting with windows APIs"}

[[crate]]
category = "Common/System"
use_case = "macOS/iOS"
recommended = ["objc2"]
notes = {"objc2": "A whole family of crates that cover binding to Objective-C and most of the Apple system APIs"}

[[crate]]
category = "Common/System"
use_case = "*nix (OSs)"
recommended = ["rustix", "nix"]
notes = {"rustix": "Efficient and safe POSIX / *nix / Winsock syscall-like APIs. It uses idiomatic Rust types: refs, slices, Results instead of raw pointers, safe wrappers around raw file descriptors, bitflags instead of bare integer flags, and several other conveniences.", "nix": "Bindings to the various *nix system functions. (Unix, Linux, MacOS, etc.)"}

[[crate]]
category = "Common/System"
use_case = "Signal handling"
recommended = ["signal-hook"]
notes = {"signal-hook": "Provides high-level, sync APIs for handling signals safely. The low-level machinery is also exposed as a separate crate, <a href=\"https://lib.rs/crates/signal-hook-registry\"><code>signal-hook-registry</code></a>, which lets other libraries like <a href=\"https://docs.rs/tokio/latest/tokio/signal\"><code>tokio::signal</code></a> interoperate with it."}

[[crate]]
category = "Common/Tooling"
use_case = "Toolchain Management"
recommended = ["rustup"]
notes = {"rustup": "Install, manage, and upgrade versions of rustc, cargo, clippy, rustfmt and more."}

[[crate]]
category = "Common/Tooling"
use_case = "Linting"
recommended = ["clippy", "cargo-semver-checks"]
notes = {"clippy": "The official Rust linter.", "cargo-semver-checks": "Lint your crate releases for semantic versioning violations."}

[[crate]]
category = "Common/Tooling"
use_case = "Code Formatting"
recommended = ["rustfmt"]
notes = {"rustfmt": "The official Rust code formatter."}

[[crate]]
category = "Common/Tooling"
use_case = "Cross Compilation"
recommended = ["cross", "cargo-zigbuild"]
notes = {"cross": "Seamless cross-compiling using Docker containers.", "cargo-zigbuild": "Easily cross-compile using Zig as the linker."}

[[crate]]
category = "Common/Tooling"
use_case = "Managing Dependencies"
recommended = ["cargo-edit", "cargo-outdated", "cargo-audit", "cargo-license", "cargo-deny"]
notes = {"cargo-edit": "Adds 'cargo upgrade' and 'cargo set-version' commands to cargo", "cargo-outdated": "Finds dependencies that have available updates", "cargo-audit": "Check dependencies for reported security vulnerabilities", "cargo-license": "Lists licenses of all dependencies", "cargo-deny": "Enforce policies on your code and dependencies."}

[[crate]]
category = "Common/Tooling"
use_case = "Testing"
recommended = ["cargo-nextest", "insta"]
notes = {"cargo-nextest": "Faster, better test runner", "insta": "Snapshot testing with inline snapshot support"}

[[crate]]
category = "Common/Tooling"
use_case = "Benchmarking"
recommended = ["criterion", "divan", "gungraun", "hyperfine"]
notes = {"criterion": "Statistically accurate benchmarking tool for benchmarking libraries", "divan": "Simple yet powerful benchmarking library with allocation profiling", "gungraun": "High-precision, one-shot and consistent benchmarking framework/harness for Rust. All Valgrind tools at your fingertips.", "hyperfine": "Tool for benchmarking compiled binaries (similar to unix time command but better)"}

[[crate]]
category = "Common/Tooling"
use_case = "Performance"
recommended = ["cargo-flamegraph", "dhat", "cargo-show-asm"]
notes = {"cargo-flamegraph": "Execution flamegraph generation", "dhat": "Heap memory profiling", "cargo-show-asm": "Print the generated assembly for a Rust function"}

[[crate]]
category = "Common/Tooling"
use_case = "Debugging Macros"
recommended = ["cargo-expand"]
notes = {"cargo-expand": "Allows you to inspect the code that macros expand to"}

[[crate]]
category = "Common/Tooling"
use_case = "Release Automation"
recommended = ["cargo-release", "Release-plz"]
notes = {"cargo-release": "Helper for publishing new crate versions.", "Release-plz": "Release Rust crates from CI with a Release PR."}

[[crate]]
category = "Common/Tooling"
use_case = "Continuous Integration"
recommended = ["rust-toolchain (github action)", "rust-cache (github action)", "install-action (github action)"]
notes = {"rust-toolchain (github action)": "Github action to install Rust components via rustup", "rust-cache (github action)": "Github action to cache compilation artifacts and speed up subsequent runs.", "install-action (github action)": "GitHub Action for installing development tools (mainly from GitHub Releases)."}

[[crate]]
category = "Networking/Async Foundations"
use_case = "General Purpose Async Executors"
recommended = ["tokio", "futures-executor", "smol"]
notes = {"tokio": "The oldest async runtime in the Rust ecosystem and still the most widely supported. Recommended for new projects.", "futures-executor": "A minimal executor. In particular, the <a href=\"https://docs.rs/futures-executor/latest/futures_executor/fn.block_on.html\">block_on</a> function is useful if you want to run an async function synchronously in codebase that is mostly synchronous.", "smol": "A small and modular async runtime. Since the implementation is modularized in smaller crates, you can directly use only the required crates instead of depending on the entire runtime."}

[[crate]]
category = "Networking/Async Foundations"
use_case = "Async Utilities"
recommended = ["futures", "async-trait"]
notes = {"futures": "Utility functions for working with Futures and Streams", "async-trait": "Provides a workaround for the lack of language support for async functions in traits"}

[[crate]]
category = "Networking/Async Foundations"
use_case = "io_uring"
recommended = ["glommio"]
notes = {"glommio": "Use if you need io_uring support. Still somewhat experimental but rapidly maturing."}

[[crate]]
category = "Networking/HTTP"
use_case = "Types & Interfaces"
recommended = ["http"]
notes = {"http": "The `http` crate doesn't actually contain an HTTP implementation. Just types and interfaces to help interoperability."}

[[crate]]
category = "Networking/HTTP"
use_case = "Low-level HTTP Implementation"
recommended = ["hyper"]
notes = {"hyper": "A low-level HTTP implementation (both client and server). Implements HTTP/1, and HTTP/2. Works best with the tokio async runtime, but can support other runtimes."}

[[crate]]
category = "Networking/HTTP"
use_case = "HTTP Client"
recommended = ["reqwest", "ureq"]
notes = {"reqwest": "Full-fat HTTP client. Can be used in both synchronous and asynchronous code. Requires tokio runtime.", "ureq": "Minimal synchronous HTTP client focused on simplicity and minimising dependencies."}

[[crate]]
category = "Networking/HTTP"
use_case = "HTTP Server"
recommended = ["axum", "actix-web"]
notes = {"axum": "A minimal and ergonomic framework. An official part of the tokio project. Recommend for most new projects.", "actix-web": "A performance focused framework. All Rust frameworks are fast, but choose actix-web if you need the absolutely maximum performance."}

[[crate]]
category = "Networking/HTTP"
use_case = "GraphQL Server"
recommended = ["async-graphql"]
notes = {"async-graphql": "A high-performance graphql server library that's fully specification compliant. Integrates with actix-web, axum, poem, rocket, tide, warp."}

[[crate]]
category = "Networking/Websockets"
use_case = "Low-level"
recommended = ["tungstenite"]
notes = {"tungstenite": "Low-level crate that others build on"}

[[crate]]
category = "Networking/Websockets"
use_case = "General Purpose"
recommended = ["tokio-tungstenite", "async-tungstenite"]
notes = {"tokio-tungstenite": "If you are using the tokio executor", "async-tungstenite": "If you are using the async-std executor"}

[[crate]]
category = "Networking/gRPC"
use_case = "General Purpose"
recommended = ["tonic"]
notes = {"tonic": "gRPC over HTTP/2 with full support for asynchronous code. Works with tokio"}

[[crate]]
category = "Networking/D-Bus"
use_case = "General Purpose"
recommended = ["zbus"]
notes = {"zbus": "A Rust-only (a)sync implementation of the protocol. Provides high-level proxies and zbus_xmlgen for scaffolding. Includes a book for D-Bus/zbus beginners."}

[[crate]]
category = "Networking/D-Bus"
use_case = "Bindings"
recommended = ["dbus"]
notes = {"dbus": "Bindings for the battle-tested libdbus implementation. Has sister crates for asynchrony, codegen, etc."}

[[crate]]
category = "Databases/SQL Databases"
use_case = "Multi Database"
recommended = ["sqlx"]
notes = {"sqlx": "Works with Postgres, MySQL, SQLite, and MS SQL.<br />Supports compile time checking of queries. Async: supports both tokio and async-std."}

[[crate]]
category = "Databases/SQL Databases"
use_case = "ORMs"
recommended = ["diesel", "sea-orm"]
notes = {"diesel": "Has excellent performance and takes an approach of strict compile time guarantees. The main crate is Sync only, but <a href=\"https://lib.rs/crates/diesel-async\">diesel-async</a> provides an async connection implementation.", "sea-orm": "Built on top of sqlx (see above). There is also a related sea-query crate that provides a query builder without full ORM functionality."}

[[crate]]
category = "Databases/SQL Databases"
use_case = "Postgres"
recommended = ["tokio-postgres"]
notes = {"tokio-postgres": "Postgres-specific library. Performs better than SQLx"}

[[crate]]
category = "Databases/SQL Databases"
use_case = "MySQL"
recommended = ["mysql_async"]
notes = {"mysql_async": "Has a poorly designed API. Prefer SQLx or Diesel for MySQL"}

[[crate]]
category = "Databases/SQL Databases"
use_case = "SQLite"
recommended = ["rusqlite"]
notes = {"rusqlite": "Provides a sync API to SQLite + provides access to advanced sqlite features."}

[[crate]]
category = "Databases/SQL Databases"
use_case = "MS SQL"
recommended = ["tiberius"]
notes = {"tiberius": "MS SQL specific library. Has better support for advanced column types than SQLx."}

[[crate]]
category = "Databases/SQL Databases"
use_case = "Oracle"
recommended = ["diesel-oci", "oracle", "sibyl"]
notes = {"diesel-oci": "Diesel backend and connection implementation for oracle databases", "oracle": "Rust bindings to ODPI-C", "sibyl": "An OCI-based interface supporting both blocking (threads) and nonblocking (async) AP"}

[[crate]]
category = "Databases/Other Databases"
use_case = "Redis"
recommended = ["redis"]
notes = {"redis": ""}

[[crate]]
category = "Databases/Other Databases"
use_case = "MongoDB"
recommended = ["mongodb"]
notes = {"mongodb": ""}

[[crate]]
category = "Databases/Other Databases"
use_case = "ElasticSearch"
recommended = ["elasticsearch"]
notes = {"elasticsearch": ""}

[[crate]]
category = "Databases/Other Databases"
use_case = "LMDB"
recommended = ["heed"]
notes = {"heed": "A fully typed LMDB wrapper with minimum overhead."}

[[crate]]
category = "Databases/Other Databases"
use_case = "Rocks DB"
recommended = ["rocksdb"]
notes = {"rocksdb": ""}

[[crate]]
category = "Databases/Other Databases"
use_case = "Cassandra"
recommended = ["cassandra-protocol", "cdrs-tokio"]
notes = {"cassandra-protocol": "Low-level Cassandra protocol implementation.", "cdrs-tokio": "High-level async Cassandra driver."}

[[crate]]
category = "Databases/Utilities"
use_case = "Connection pool"
recommended = ["deadpool"]
notes = {"deadpool": "A dead simple async pool for connections and objects of any type."}

[[crate]]
category = "CLIs/Argument Parsing"
use_case = "Fully-featured"
recommended = ["clap"]
notes = {"clap": "Ergonomic, battle-tested, includes the kitchen sink, and is fast at runtime. However compile times can be slow"}

[[crate]]
category = "CLIs/Argument Parsing"
use_case = "Minimal"
recommended = ["lexopt", "pico-args"]
notes = {"lexopt": "Fast compile times, fast runtime, pedantic about correctness. API is less ergonomic", "pico-args": "Fast compile times, fast runtime, more lax about correctness. API is more ergonomic"}

[[crate]]
category = "CLIs/Utility"
use_case = "Globbing"
recommended = ["globset"]
notes = {"globset": "High-performance globbing that allows multiple globs to be evaluated at once"}

[[crate]]
category = "CLIs/Utility"
use_case = "Directory walking"
recommended = ["walkdir", "ignore"]
notes = {"walkdir": "Basic recursive filesystem walking.", "ignore": "Recursive filesystem walking that respects ignore files (like .gitignore)"}

[[crate]]
category = "CLIs/Utility"
use_case = "File watching"
recommended = ["notify"]
notes = {"notify": "Watch files or directories and execute a function when they change"}

[[crate]]
category = "CLIs/Utility"
use_case = "User directories"
recommended = ["dirs", "directories"]
notes = {"dirs": "Provide platform-specific locations for configuration, cache, and other data", "directories": "A higher-level library that can also compute paths for applications"}

[[crate]]
category = "CLIs/Terminal Rendering"
use_case = "Coloured Output"
recommended = ["termcolor"]
notes = {"termcolor": "Cross-platform terminal colour output"}

[[crate]]
category = "CLIs/Terminal Rendering"
use_case = "Progress indicators"
recommended = ["indicatif"]
notes = {"indicatif": "Progress bars and spinners"}

[[crate]]
category = "CLIs/Terminal Rendering"
use_case = "TUI"
recommended = ["ratatui", "crossterm"]
notes = {"ratatui": "A high-level TUI library with widgets, layout, etc. ", "crossterm": "Low-level cross-platform terminal rendering and event handling"}

[[crate]]
category = "CLIs/Terminal Rendering"
use_case = "Interactive prompts"
recommended = ["inquire"]
notes = {"inquire": "Ask for confirmation, selection, text input and more"}

[[crate]]
category = "Concurrency/Data Structures"
use_case = "Mutex"
recommended = ["parking_lot"]
notes = {"parking_lot": "std::sync::Mutex also works fine. But Parking Lot is faster."}

[[crate]]
category = "Concurrency/Data Structures"
use_case = "Atomic pointer swapping"
recommended = ["arc-swap"]
notes = {"arc-swap": "Useful for sharing data that has many readers but few writers"}

[[crate]]
category = "Concurrency/Data Structures"
use_case = "Concurrent HashMap"
recommended = ["dashmap", "papaya"]
notes = {"dashmap": "The fastest for general purpose workloads", "papaya": "Particularly good for read-heavy workloads."}

[[crate]]
category = "Concurrency/Data Structures"
use_case = "Channels"
recommended = ["crossbeam-channel", "flume", "tokio", "postage"]
notes = {"crossbeam-channel": "The same channel algorithm as in the standard library but with a more powerful API, offering a Go-like 'select' feature.", "flume": "Smaller and simpler than crossbeam-channel and almost as fast", "tokio": "Tokio's sync module provides channels for using in async code", "postage": "Channels that integrate nicely with async code, with different options than Tokio"}

[[crate]]
category = "Concurrency/Data Structures"
use_case = "Parallel computation"
recommended = ["rayon"]
notes = {"rayon": "Convert sequential computation into parallel computation with one call - `par_iter` instead of `iter`"}

[[crate]]
category = "Graphics/GUI"
use_case = "GTK"
recommended = ["gtk4", "relm4"]
notes = {"gtk4": "Rust bindings to GTK4. These are quite well supported, although you'll often need to use the C documentation.", "relm4": "A higher-level library that sits on top of gtk4-rs"}

[[crate]]
category = "Graphics/GUI"
use_case = "Frontend Web GUI"
recommended = ["leptos", "dioxus-web"]
notes = {"leptos": "", "dioxus-web": ""}

[[crate]]
category = "Graphics/GUI"
use_case = "Webview-based GUI"
recommended = ["tauri", "dioxus-desktop"]
notes = {"tauri": "Electron-like web-based UI. Except it uses system webviews rather than shipping chromium, and non-UI code is written in Rust rather than node.js", "dioxus-desktop": "Uses the same webview as Tauri for rendering but the UI is defined in Rust/Dioxus rather than JavaScript. Dioxus runs outside the webview with the framework handling IPC to update the DOM"}

[[crate]]
category = "Graphics/GUI"
use_case = "Native GUI"
recommended = ["iced", "egui", "slint", "gpui"]
notes = {"iced": "Retained mode UI with a nice API. ", "egui": "Immediate-mode UI with lots of widgets. Very useable out of the box if your needs are simple and you don't need to customise the look and feel", "slint": "A stable and feature-rich UI toolkit with a declarative language. Licensed under GPL, royalty-free for desktop/mobile, or with paid options for proprietary embedded use.", "gpui": "High performance framework used in the Zed text editor. However, focus has recently moved away from community contributions."}

[[crate]]
category = "Graphics/GUI"
use_case = "Window creation"
recommended = ["winit"]
notes = {"winit": "The defacto standard option. Uses an event loop based architecture. Widely used and should probably be the default choice."}

[[crate]]
category = "Graphics/GUI"
use_case = "2D Renderers"
recommended = ["skia-safe", "vello", "vello_cpu"]
notes = {"skia-safe": "Bindings to the Skia C++ library. The most complete option with excellent performance. However, it can be difficult to get it to compile.", "vello": "WGPU based and uses cutting edge techniques to render vector paths using the GPU.", "vello_cpu": "Pure-CPU (no GPU) rendering with excellent performance (multithreaded and SIMD-accelerated."}

[[crate]]
category = "Graphics/GUI"
use_case = "UI layout"
recommended = ["taffy", "morphorm"]
notes = {"taffy": "Supports Flexbox and CSS Grid algorithms.", "morphorm": "Implements an algorithm based on Figma's AutoLayout"}

[[crate]]
category = "Graphics/GUI"
use_case = "Text layout"
recommended = ["parley", "cosmic-text"]
notes = {"parley": "Supports advanced layout features like laying out boxes inline with text.", "cosmic-text": "Has better support for large buffers of monospace text"}

[[crate]]
category = "Graphics/GUI"
use_case = "Accessibility"
recommended = ["accesskit"]
notes = {"accesskit": "Allows you to export a semantic tree representing your UI to make accessible to screen readers and other assistive technologies"}

[[crate]]
category = "Graphics/GUI"
use_case = "Clipboard"
recommended = ["arboard"]
notes = {"arboard": "A fork of rust-clipboard that supports copy and pasting of both text and images on Linux (X11/Wayland), MacOS and Windows."}

[[crate]]
category = "Graphics/GUI"
use_case = "File Dialogs"
recommended = ["rfd"]
notes = {"rfd": "Platform-native open/save file dialogs. Can be used in conjunction with other UI libraries."}

[[crate]]
category = "Graphics/Game Development"
use_case = "Game Engines"
recommended = ["bevy", "fyrox", "ggez", "macroquad"]
notes = {"bevy": "An ECS based game engine, good for 3D but also capable of 2D.", "fyrox": "An OOP-focused game engine with 3D and 2D support and a full GUI scene editor.", "ggez": "A simpler option for 2d games only.", "macroquad": "A simple and easy to use 2d game library, great for beginners."}

[[crate]]
category = "Graphics/Game Development"
use_case = "3D Math"
recommended = ["glam"]
notes = {"glam": "Fast math library optimised for game development use cases"}
