╔════════════════════════════════════════════════════════════════════════╗
║                    🥾 FZF MENU - ZELLIJ LAUNCHER                         ║
╚════════════════════════════════════════════════════════════════════════╝

✅ SUCCESS! fzf menu launched in Zellij floating pane!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 CURRENT STATE:

  Pane ID: terminal_5
  Command: bash _b00t_/scripts/zellij-fzf-menu.sh
  Location: Floating pane (60% width, 50% height)
  Status: Running

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 HOW IT WORKS:

The key difference: using `zellij run` instead of `write-chars`

  ❌ write-chars: Just types text into an existing pane
  ✅ zellij run: Creates a new pane with proper TTY and runs the command

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 COMMAND USED:

  zellij run --floating --width 60% --height 50% \
    --cwd /home/brianh/.dotfiles \
    -- bash _b00t_/scripts/zellij-fzf-menu.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 MENU OPTIONS (in floating pane):

  > 1 • Tell me a joke • Programming humor
    2 • Show system status • Check b00t health
    3 • View recent tasks • Task history
    4 • Run tests • Execute test suite
    5 • Deploy to production • Deploy code
    6 • Exit menu • Return to workspace

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⌨️ KEYBOARD NAVIGATION:

  ↓ / j       → Move down
  ↑ / k       → Move up
  Enter       → Select option
  ESC         → Cancel and close
  Tab         → Next match
  Shift+Tab   → Previous match
  Type text   → Filter options in real-time

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎨 FZF FEATURES:

  ✅ Interactive selection (highlighted with >)
  ✅ Real-time filtering (type to search)
  ✅ Smooth navigation (arrows or vi-style)
  ✅ Visual feedback (color-coded)
  ✅ Rounded border with label
  ✅ Auto-complete (Tab for next match)
  ✅ Adaptive height (adjusts to content)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 USAGE EXAMPLES:

1. Navigate with arrow keys or j/k
2. Type "status" to filter to system status option
3. Type "joke" to filter to joke option
4. Type "deploy" to filter to deployment option
5. Press Enter to select and execute
6. Press ESC to cancel and close the pane

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 LAUNCH COMMANDS:

  # Direct launch
  zellij run --floating --width 60% --height 50% \
    --cwd /home/brianh/.dotfiles \
    -- bash _b00t_/scripts/zellij-fzf-menu.sh

  # Via wrapper script
  bash _b00t_/scripts/zellij-run-fzf-menu.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

╔════════════════════════════════════════════════════════════════════════╗
║                    ✅ FZF MENU NOW RUNNING!                             ║
║                                                                          ║
║  Look at your Zellij session - you should see a floating pane with     ║
║  the interactive fzf menu. Use arrow keys, type to filter, and select! ║
╚════════════════════════════════════════════════════════════════════════╝