[b00t]
name = "hive"
type = "cli"
hint = "b00t hive — CMDB: system resource state, profile activation, command guards"
unlocks = ["b00t hive *"]
desires = "0.8"
lfmf_category = "hive-cmdb"

[b00t.learn]
inline = "hive status shows RAM/CPU/GPU. hive list shows 14 profiles (inference-qwen3, download-mode, etc.). hive activate <profile> transitions system state through resource gate checks. See hive-guards.hive.toml for universal command guards."

# b00t:map v1
# summary: b00t hive CLI datum — CMDB resource management and profile activation
# tags: b00t, hive, cmdb, profile, guard, system
# tier: frontier
# cmds: b00t hive status, b00t hive list, b00t hive activate inference-sm0l
# complexity: 4
