---
tool-schema-requirements: opencode Zod validator rejects tools without description, inputSchema without type:object, or with $schema field. Must pin protocol to client version (2024-11-05). Use rmcp SDK to avoid these by construction.
