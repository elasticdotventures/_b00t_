---
rhai-execution: RHAI script execution requires OPERATOR_ACTION env var, not positional args

---
rhai-paths: RHAI scripts live in /home/brianh/.dotfiles/_b00t_/scripts/, not .b00t/_b00t_/scripts/

---
Merge window script at _b00t_/scripts/merge-window.sh: executable bash with set -euo pipefail, git submodule foreach fetch+status, sed version bump (0.7.49 → 0.8.0-pre.0), [y/N] before push, early exit if clean. SCM conventions datum at _b00t_/schema/SCM-CONVENTIONS.tomllmd: mdtable with commit_type/file_count_min/file_count_max/branch_prefix/security_tier/allowed_scopes/description for fix/feat/refactor/chore/docs/test/perf/style/sec types. Both files need terminal access to create.

---
operator kaizen: Before spawning a worker agent: 1) checkpoint git state 2) load relevant skills for the worker via b00t learn 3) provide domain context (irontology, ledgrrr, etc.) beyond model cutoff 4) bless with explicit auth/tools 5) after worker completes, verify tests pass independently 6) spawn adversarial reviewer (bouncer) with same context + diff 7) iterate ralph loop until worker happy + tests pass. Never just "delegate and forget".

---
operator kaizen: checkpoint git state before spawning worker agents. Load relevant skills (irontology, crypto, etc.) via b00t learn so the worker has domain context beyond model cutoff. Bless workers with explicit auth, tools, and resource paths. Verify tests pass independently after worker completes. Spawn adversarial reviewer (bouncer) with same context + diff for good faith review. Run ralph loop until worker satisfied and tests pass.
