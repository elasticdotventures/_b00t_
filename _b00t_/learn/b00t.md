---
rhai-execution: RHAI script execution requires OPERATOR_ACTION env var, not positional args

---
rhai-paths: RHAI scripts live in /home/brianh/.dotfiles/_b00t_/scripts/, not .b00t/_b00t_/scripts/

---
---
debugging-ux: Always bump the version/build-counter as the very first step in any compile/run test loop. This proves monotonic progress to b00t's watchdog and prevents the guard from killing the attempt. The version should appear in the DOM dump, window titlebar, and SLO file every artifact is traceable to a unique build number.
debugging-ux: Always bump the version/build-counter as the very first step in any compile/run test loop. This proves monotonic progress to b00t's watchdog and prevents the guard from killing the attempt. The version should appear in the DOM dump, window titlebar, and SLO file so every artifact is traceable to a unique build number.

---
sfw system normal: SFW variant is system normal minimum surface. Documentation engine shared with ledgrrr toolchain. b00t and ledgrrr harmonize may merge someday.

---
governance gate architecture: 3-return gates (Allow/Deny/Hook) with Eisenhower 4-quadrant routing are correct for agent action control. TOML gate config format is good and reusable. Bash implementation is brittle (49 dead variables, triplicated menu items, no test harness) — Rust gate system is the target state.

---
compound-engineering parity: b00t covers 80% of everyinc/compound-engineering-plugin workflow via skills+CLI+MCP. Gaps: doc-review skill, ideate command, time-windowed health. b00t strengths CE lacks: typed pipelines, WASM codegen, twin simulation, container stack. Architecture is Rust CLI vs TS plugin — complementary not competitive.

---
canonical types: ufo-types::dare (DaredProposal, OodaStateMachine, OodaPhase, OodaGuards) — OODA state-change workflow types now belong to the crate, not hand-rolled shapes here.

---
shortcut tokens: Prefer idiomatic command words like sh over symbolic or terse aliases; avoids bash history expansion and ambiguous short-code semantics.

---
desired-state: b00t-admin merges with ledgrrr as shared lib; isometric/kasuari constraint layout replaces mermaid; SemanticClass display system with css_class animation hooks; DatumType::implies() stereotype hierarchy; blessed ecosystem search with trust tiers; OODA typed workflow contracts with governance; b00t sh guard-enforced sandbox; build daemon auto-restart; deploy-check compile-time HTML tests; CDP browser monitoring; Pinia/localStorage UI state persistence

---
session-retrospective: (1) Template-scope errors — JS statements inside object literals pass node -c but fail in browser. Only CDP/jsdom catches them. Add to deploy-check. (2) 900MB workspace kills build speed — build daemon (b00t-buildd) partially solves but needs smarter caching. (3) Datum type suffixes cause disambiguation — implies() hierarchy + symlinks fix but capability dispatch is the long-term solution. (4) MCP server compat: opencode Zod validator rejects tools without description and inputSchema without type:object. rmcp SDK avoids these. (5) async-openai hangs on custom HTTP endpoints — use raw reqwest for embedding calls. (6) PyPI always returns HTTP 200 even for Not Found — check JSON body. (7) Git merge conflicts in format! templates survive into served HTML — html_sanity compile-time test catches these. (8) SemanticClass display system is right pattern — per-variant hardcoding is wrong. (9) TOML overrides for display were too verbose — code is the single source of truth. (10) Submodule branch tracking with git submodule update --remote prevents detached HEAD drift.
