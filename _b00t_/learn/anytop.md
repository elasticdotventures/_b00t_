---
adoption boundary: AnyTop is a topology-conditioned PyTorch and BVH research generator, not a process registry. Normalize trusted cond.npy topology to ArticulatedBodyV1, keep generated motion in BVH or MotionClipV1, export static rest structure to URDF, and require calibration inputs for reverse conversion.
