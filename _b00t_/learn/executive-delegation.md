---
ch0nky (localhost: 8001) is always live. Use `just delegate task=...` for implement/refactor/debug. Use `just ask query=...` for classify/summarize. Use `just check-fast` before `cargo test`. Never spawn frontier sub-agents for ch0nky work. Only return PASS/FAIL + 5-line summary to executive context.
