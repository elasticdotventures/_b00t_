---
gh copilot explain cannot power a PR review workflow: Actions GITHUB_TOKEN has no Copilot entitlement, explain sees file heads not diffs, and an empty-match grep made it spam every PR sync — deleted copilot-review.yml
