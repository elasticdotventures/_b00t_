# Handoff — 2026-07-13

## Mission
Release 0.10.0: pipeline engine + `--agent` alias + release process hardening.

## What was done

### `--agent` alias for `--role`
- `b00t whoami --agent <role>` now works (alias for `--role`)
- `b00t blessing --manifest --agent <role>` now works
- `b00t gap_detect --agent <role>` now works
- No internal rename — `--role` field name preserved, `--agent` is a clap alias

### Fix-forward bugs for 0.10.0
- `k0mmand3r` crate: workspace version 0.9.9→0.10.0 broke `b00t-lib-chat`'s `k0mmand3r = "^0.9"` constraint. Fixed to `"^0.10"`.
- `pipeline_flowctl.rs`, `pipeline_cache.rs`: `gen` keyword collision, `{N}` format string, missing `flow_control` field in test constructors — all fixed by scheduler agent.
- `pipeline_executor.rs`: stray brace, missing `flow_gates` field, `Mutex` deref — fixed.
- `pipeline_k8s.rs`: `serde_json::json!` macro recursion → added `#![recursion_limit = "256"]` to `lib.rs`.

### Release process enhancement (`justfile`)
- New recipe: `just pre-release-check` — 5-step validation gate:
  1. `cargo check --workspace` — 0 errors
  2. `--agent` alias resolution for `whoami`
  3. `--agent` alias resolution for `blessing`
  4. Workspace version consistency (all `version.workspace = true` crates)
  5. `cargo test --no-run` — test compilation
- `just release` now runs `just pre-release-check` before dispatching

### Pipeline engine (31 issues from previous session)
Completed in the prior session: 11 commits, 26 source modules, 22 test files, 0 errors.
See PR #779 and `_b00t_/learn/handoff-2026-07-12.md`.

## Current branch: `feat/pipeline-engine`
12 commits ahead of main (11 pipeline engine + 1 release process):
```
322ac2a8 feat: add --agent alias for --role; fix-forward deps for 0.10.0; enhance release process
b28368de chore: bump version to 0.10.0 for pipeline engine release
87332b76 feat: add JWT auth, K8s CRD, and state machine modules (#738, #731, #743)
4e683c09 feat(pipeline): expose store methods as NATS request-reply subjects
25be8c7c feat(pipeline): complete resource-aware scheduler (#727)
00ca496b feat: add BPipelineCommand to b00t-mcp for pipeline lifecycle (#736, #717)
...
```

## PR
- **#779**: Pipeline engine mega-PR (feat/pipeline-engine → main)
- **#755**: lib.rs split (merged)

## Next steps
1. Merge PR #779 to main
2. Run `just release` to tag and dispatch GitHub release workflow
3. Verify `--agent` alias in production: `b00t whoami --agent worker`
