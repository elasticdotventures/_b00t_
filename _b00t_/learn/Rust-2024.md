---
edition migration: Rust 2024 makes std::env::set_var/remove_var unsafe (E0133). In test code, wrap in unsafe {}. #![allow(...)] should be removed entirely (not converted to #![expect(...)]) — expect warns when the lint doesn't fire, indicating the allow was unnecessary. k0mmand3r crate's 4 #![allow] lines were all spurious.
