---
gh pr edit may fail on the deprecated Projects (classic) GraphQL field even for title/body-only edits; use gh api: gh pr edit may fail on the deprecated Projects (classic) GraphQL field even for title/body-only edits; use gh api -X PATCH repos/OWNER/REPO/pulls/NUMBER with explicit fields as the narrow fallback. <!-- salvaged:no_colon -->
