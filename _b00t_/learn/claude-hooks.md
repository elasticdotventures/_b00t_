---
cbm-code-discovery-gate PreToolUse hook over-blocks: denies Read on prose/markdown (AGENTS/*.md, memory index) that has no code-graph representation; gate should allowlist non-code extensions/paths or check graph coverage before blocking
