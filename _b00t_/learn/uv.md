---
ALWAYS use `uv pip` not `pip` — b00t guard: pip is sunset. Command: `uv pip install pkg --python /home/brianh/.venv/bin/python3`

---
python package install: Always uv pip install, never pip. b00t guards block raw pip. Form: uv pip install <pkg>.
