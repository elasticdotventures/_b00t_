---
hive scope: b00t hive manages LOCAL system state (RAM/GPU/services on this metal node). The HIVE is the network of such nodes sharing a repo — each node runs its own 'b00t hive status'. Executive agents coordinate across nodes via agent delegation; the local hive is one node's CMDB view exposed to the hive network
