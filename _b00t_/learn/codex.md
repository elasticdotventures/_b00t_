---
mcp-add-syntax: current Codex MCP CLI supports codex mcp add <name> -- <command> <args...>; older b00t code using codex mcp add-json --repo/--user fails because add-json and repo/user flags are no longer recognized.
