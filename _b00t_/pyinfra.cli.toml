# pyinfra.cli — cli datum
# Assimilated from: github:pyinfra-dev/pyinfra

[b00t]
name        = "pyinfra"
type        = "cli"
hint        = "pyinfra — pyinfra-dev/pyinfra — pyinfra-dev/pyinfra"

install = "uv pip install pyinfra"
version = "pyinfra --version"
# version_regex = '(\\d+\\.\\d+\\.\\d+)'

# b00t:map v1
# summary: datum for github:pyinfra-dev/pyinfra — scaffolded by grok assimilate
# tags: pyinfra, pyinfra-dev, pyinfra
# tier: ch0nky
# cmds: b00t install pyinfra
# complexity: 1
