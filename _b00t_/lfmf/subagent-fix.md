# subagent-fix

Operators can spawn sub-agents to `--fix-in-flight` (hotfix during active operation) or wait until the ship is on the ground (deferred fix). In-flight fixes carry risk but unblock operations. Ground fixes are safer but delay resolution.
