[project]
name = "b00t-aws-tools"
version = "0.1.0"
description = "Unified AWS tool generators and helpers for the b00t hive."
requires-python = ">=3.12"
dependencies = [
    "boto3",
    "pydantic",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
