[package]
name = "blessed"
version = "0.1.0"
edition = "2021"
description = "Query the blessed.rs crate directory — curated recommendations for the Rust ecosystem"
license = "MIT"
repository = "https://github.com/elasticdotventures/_b00t_"
keywords = ["blessed", "crates", "ecosystem", "recommendations"]
categories = ["development-tools"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
