[package]
name = "b00t-pipeline-types"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Lightweight standalone pipeline types (StageSpec, CapsuleProfile, etc.) extracted from b00t-cli — mirroring the ufo-types precedent"

[dependencies]
serde = { workspace = true, features = ["derive"] }
anyhow.workspace = true
b00t-c0re-lib.workspace = true

[dev-dependencies]
serde_json = "1.0"
toml = "0.8"
